# Transformer

The Transformer replaces recurrence with attention mechanisms in its encoder-decoder architecture.

Self-attention lets positions combine information from other positions, while positional encodings supply order information. Multi-head attention learns several projections. The original architecture also uses feed-forward layers and residual connections. Parallel training over positions differs from sequential autoregressive generation.

Primary source: https://arxiv.org/abs/1706.03762
